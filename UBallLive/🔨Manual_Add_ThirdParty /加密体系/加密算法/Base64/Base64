
iOS 提供了Base64加密的api、使用非常简单
常见的加密有Base64加密和MD5加密,有标准的编码表，任何人都可以解密,通常用于隐藏明文,具体的加密方案需要另行处理
Base64加密本质上只是一种编码方式
Base64加密是可逆的，MD5加密目前来说一般是不可逆的
Base64是开发中使用较替换的加密算法（严格来说不是加密，编码）

1、Base是使用多个交替的编码方式
2、可以将二进制数据转换成二进制
3、编码后的结果是由a-z，A-Z，+ / 64个不同的字符组成，以=结尾，但等号不是一定会出现的
